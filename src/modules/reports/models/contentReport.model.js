import mongoose from 'mongoose';
const contentReportSchema = new mongoose.Schema({ reporter: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, targetType: { type: String, enum: ['user', 'post', 'business', 'group', 'story', 'message', 'marketplace_item'], required: true, index: true }, targetId: { type: mongoose.Schema.Types.ObjectId, required: true, index: true }, reason: { type: String, enum: ['spam', 'scam', 'harassment', 'violence', 'nudity', 'illegal_content'], required: true, index: true }, details: { type: String, default: '', maxlength: 1000 }, status: { type: String, enum: ['open', 'reviewing', 'resolved', 'rejected'], default: 'open', index: true }, reviewedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', default: null }, reviewedAt: { type: Date, default: null } }, { timestamps: true });
contentReportSchema.index({ status: 1, createdAt: -1 });
export const ContentReport = mongoose.model('ContentReport', contentReportSchema);
